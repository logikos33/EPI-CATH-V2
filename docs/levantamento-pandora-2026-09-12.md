# Levantamento Pandora RVB → Pandora da feira

**Data:** 2026-09-12 · **Escopo:** inventário com prova, para provisionamento do Pandora novo (Jetson Orin NX 8GB, JetPack 6.2 alvo) e para a tabela de dimensionamento do plano de negócio. ⛔ Nada foi consertado, treinado ou otimizado nesta rodada.

---

## ⚠️ Divergências entre o prompt e a realidade medida

O prompt já avisava para tratá-lo como hipótese. Quatro pontos vieram de leitura desatualizada e foram corrigidos aqui:

| # | O prompt dizia | O que foi medido | Impacto |
|---|---|---|---|
| 1 | Repo local em `~/Logikos Recogntion/Recognition`, worktree próprio | O checkout compartilhado estava **460 commits atrás** de `origin/develop`, com mudanças soltas não commitadas | Todo trabalho saiu de um worktree novo em `~/Logikos-mutirao/wt-pandora`, a partir de `origin/develop@83c41159` |
| 2 | Runners de produção vivem em `/home/pandora/jetson-experiments/mm/` | Aquele diretório é a **campanha de shootout/escala de julho**, já documentada em `docs/decisions/` — não é produção. O runner de produção real roda via systemd (`recognition-infer@epi.service`) a partir de `/home/pandora/.config/recognition/deepstream/app_epi.txt`, fora de `jetson-experiments/` | Bloco 0 inteiro foi reapontado para o lugar certo — ver abaixo |
| 3 | "28 câmeras a 42% de GPU foram medidas em substream 480p" | Produção RVB roda **18 câmeras no stream PRINCIPAL** (`subtype=0`, não substream), streammux normalizando para **1280×720**, GPU médio 29,9% (pico 98%) | O número da campanha (480p) e o de produção (720p) não são a mesma coisa — não podem ser somados sem essa ressalva |
| 4 | "Observabilidade já instalada (VictoriaMetrics/Perses/tegrastats_exporter)" | **Nenhuma dessas portas está aberta no box** (`ss -ltn` confirma). A telemetria real é local: `edge-telemetry-collector.service` (tegrastats → JSONL) e `edge-monitoring-collector.service` (7 camadas → SQLite ring buffer local), sem stack de métricas externa rodando | Bloco 1 usa amostra passiva direta (tegrastats, 19 amostras, 19s), não uma dashboard pronta |

---

## Bloco 0 · Runners versionados

**PR aberto:** #927 (`levantamento/pandora-feira-2026-09-12` → `develop`) · **Issue:** #926 (achado) · **Issue:** #928 (drift descoberto no caminho)

### O runner de produção real

`recognition-infer@epi.service` (systemd --user) → `deepstream-app -c /home/pandora/.config/recognition/deepstream/app_epi.txt`. Publicador de detecções: `recognition-deteccoes@epi.service` → `publicar_deteccoes.py`.

| runner/artefato | cenário | caminho no box | segredo? | versionado agora em |
|---|---|---|---|---|
| config do deepstream-app | epi (produção) | `/home/pandora/.config/recognition/deepstream/app_epi.txt` | ✅ RTSP com usuário:senha — **redigido antes do commit** | `deepstream/epi/app_epi.txt` |
| índice fonte→câmera | epi | `.../fontes_epi.json` | não | `deepstream/epi/fontes_epi.json` |
| cooldown por câmera | epi | `~/.config/recognition/cooldown_por_camera.json` | não | `deepstream/epi/cooldown_por_camera.json` |
| taxonomia | epi | `~/recognition/models/mapa_taxonomia_epi.json` | não | `deepstream/epi/mapa_taxonomia_epi.json` |
| nvinfer do modelo servido | epi | `~/recognition/models/config_infer_46a30ed9.txt` | não | `deepstream/epi/config_infer_46a30ed9.txt` |
| labels do modelo servido | epi | `~/recognition/models/labels_46a30ed9.txt` | não | `deepstream/epi/labels_46a30ed9.txt` |
| 🔴 **parser bbox RF-DETR (fonte)** | epi | `~/jetson-experiments/rfdetr-parser/nvdsparsebbox_rfdetr.cpp` + `Makefile` | não | `deepstream/epi/custom_parsers/` — **maior severidade: só existia binário compilado no box** |
| amostragem de classe×confiança | epi (operação) | `~/recognition/bin/amostrar_classes.sh` | não | `deepstream/epi/scripts/amostrar_classes.sh` |
| degrau de medição de capacidade | epi (operação) | `~/recognition/degrau.sh` | não | `deepstream/epi/scripts/degrau.sh` |
| amostra passiva (tegrastats) | epi (operação) | `~/recognition/amostra.sh` | não | `deepstream/epi/scripts/amostra.sh` |
| gerador de config deepstream | epi | `~/recognition/gerar_config_deepstream.py` | não | ⚠️ **JÁ versionado** em `deployments/edge/gerar_config_deepstream.py` — box diverge (311 linhas de diff, ver #928) |
| publicador de detecções | epi | `~/recognition/bin/publicar_deteccoes.py` | não | ⚠️ **JÁ versionado** em `deployments/edge/publicar_deteccoes.py` — box diverge (195 linhas de diff, hotpatch de 09/09, ver #928) |

`deepstream/{epi,quality,fueling}/` no repo tinha só `.gitkeep` — confirmado (`git ls-files`). `deepstream/shared/custom_parsers/` já tinha o parser genérico YOLOX (Makefile, README, template, `.cpp`) — isso estava certo e não mudou.

**Não copiado, de propósito:** chaves de device (`~/.config/recognition/keys/*.pem`, `identity.json`) e `edge-sync-agent.env` — são segredo de device/identidade, não configuração de cenário; não pertencem a repositório algum.

---

## Bloco 1 · Capacidade medida (produção real, sem carga sintética)

**Janela:** amostra passiva de 19s (tegrastats, `--interval 1000`) sobre o processo já rodando + 1 contagem de fontes ativa em `ss`/`app_epi.txt`. Sem tocar câmera/DVR.

| O quê | Medido |
|---|---|
| Câmeras ativas | **18** fontes configuradas (`grep -c "^\[source" app_epi.txt`), **17** mapeadas em `fontes_epi.json` (fonte 17 sem `camera_id` — observado, não investigado) |
| Resolução por stream | Todas as 18 no **stream principal** (`subtype=0` — dialeto Dahua/Intelbras, não substream). `[streammux]` normaliza para **1280×720**, `batch-size=18` |
| Modelos por câmera | 1 modelo (RF-DETR `model_46a30ed9`) para todas as 18, `network-type=0`, `interval=0` (infere todo frame) |
| FPS real | ⛔ **não encontrado nesta rodada** — não há contador de FPS-por-stream exposto sem instrumentar o pipeline; fora do escopo (não é benchmark novo) |
| GPU | **média 29,9%** (min 0%, max **98%**) — 19 amostras, 1/s |
| CPU | **média 11,0%** (todos os 8 núcleos, todas as amostras), pico pontual 26% |
| RAM | **5.247 / 15.656 MB** (33,5%) |
| 🔴 NVDEC | ⛔ **não confirmado.** `tegrastats` não expôs campo NVDEC/NVENC nas amostras colhidas, e não há `nvv4l2decoder`/`avdec_h264` nos logs de inferência disponíveis (`grep` vazio). Item aberto — **resultado negativo, não investigação completa** (confirmar exigiria instrumentar o pipeline GStreamer, fora do escopo desta rodada) |
| Janela da métrica | Amostra pontual de 12/09, ~16h09 (horário do box). ⚠️ O DVR grava por movimento — não há garantia de que este instante representa o pico real do dia |

⚠️ **O que NÃO é derivável daqui:** RVB não roda módulo de fogo, e roda a 720p (não 480p). A calibragem fogo+EPI juntos, na resolução de produção, só sai medindo no Pandora novo.

---

## Bloco 2 · Ambiente e artefatos de provisionamento

| Componente | RVB hoje | Pandora da feira (alvo) | Precisa regerar? |
|---|---|---|---|
| JetPack / L4T | **R36.4.3** (= JetPack 6.2, confirmado via `/etc/nv_tegra_release`) | JetPack 6.2 | **Não**, se o flash usar a mesma imagem — mesma major/minor já |
| CUDA | **12.6** | (herdado do JetPack 6.2, deve bater) | A confirmar no flash real |
| TensorRT | **10.3.0.30** | (idem) | A confirmar no flash real |
| DeepStream | **7.1** (GCID 37774586, 04/out/2024) | (idem, se instalado o mesmo pacote) | A confirmar |
| Engine do modelo (`.engine`) | `model_46a30ed9_fp16_b1.engine`, buildado com `trtexec --onnx=model_46a30ed9.onnx --fp16 --memPoolSize=workspace:2048M` (TensorRT v10.3.0, batch implícito=1) | — | **SIM, sempre** — engine não é portável entre placas físicas, mesmo com JetPack idêntico. Regenerar a partir do `.onnx` (que É portável) |
| Parser bbox customizado (`.so`) | `libnvdsparsebbox_rfdetr.so`, compilado localmente | — | **SIM** — mas a fonte agora está versionada (`deepstream/epi/custom_parsers/`), então é `make` no destino, não reconstrução do zero |

**Modelo em uso:** `model_46a30ed9.onnx` (RF-DETR, Apache 2.0) · engine FP16 batch=1 · `num-detected-classes=13` no config (nota do próprio arquivo: o tensor real tem 11 — ver `docs/decisions/` sobre esse modelo, achado anterior a esta rodada) · 13 rótulos em `labels_46a30ed9.txt` · `pre-cluster-threshold=0.25` · pré-processamento RGB + ImageNet mean/std, resolvido por medida em 09/09 (comentário no próprio `config_infer_46a30ed9.txt`, correspondente ao D-66 do registro de decisões).

---

## Bloco 3 · O que a ferramenta já faz

### 3.1 · Estúdio de cena (customização por câmera)

| Item | Existe? | Onde (file:line) | O que falta |
|---|---|---|---|
| Modelo/classes por câmera (atribuição simples por módulo) | ✅ sim | services/api/app/api/v1/cameras/model_handlers.py:209-304 (GET/PUT /api/cameras/<id>/models); apps/frontend/src/components/cameras/CameraModelAssignment.tsx:65-175 (3 selects EPI/Qualidade/Contagem); montado em apps/frontend/src/pages/CamerasPage.tsx:326 | nada — cobre atribuição de modelo por módulo (epi/quality/counting), admin-only, com validação de módulo do modelo (model_handlers.py:98-110) e da câmera (model_handlers.py:78-95) |
| Modelo/classes por câmera (registry-level: geometria + classes habilitadas + thresholds por classe) | ✅ sim | services/api/app/api/v1/cameras/model_config_handlers.py:143-374 (GET/POST/history/rollback /api/cameras/<id>/model-config); validação em services/api/app/domain/services/geometry_validation.py:30-77 (roi/line/classes/thresholds por classe) | não encontrei consumidor no frontend desta rota /model-config (grep em apps/frontend/src não retornou uso de 'model-config'/'model_config' fora dos arquivos de backend/testes) — API pronta, tela não localizada |
| Agendamento por módulo (camera_modules_schedule / schedule_rules) | ⚠️ parcial | services/api/app/api/v1/cameras/module_handler.py:137-203 (PUT /api/cameras/<id>/schedule + GET /module/current, valida via app/domain/services/camera_module_service.py:validate_schedule_rules/resolve_active_module) | nenhuma tela encontrada no frontend: grep por 'schedule_rules', 'agendamento', 'putCameraSchedule', '/schedule' em apps/frontend/src não achou nenhum componente ou service consumindo esse endpoint — só o módulo 'ativo agora' (active_module) é editado via patch_camera_module, sem UI de regras de horário |
| Vínculo câmera↔módulo (para que serve a câmera — N:N, migration 134) | ✅ sim | services/api/app/api/v1/cameras/modules_handler.py:87-199 (GET/PUT /api/cameras/modules); apps/frontend/src/app/estudio/CamerasPorModulo.tsx (tela completa, grade com chips por câmera) | nada — a própria tela documenta (linhas 28-31) que a coleta/dashboard ainda não leem esse vínculo, mas isso é limitação de efeito downstream, não da customização em si |
| Zonas/ROI por câmera (zona de exclusão, linha, ponto — 'Cenário/Operação' ADR-0032) | ✅ sim | apps/frontend/src/components/scenario/ScenarioEditor.tsx:1-150 (editor visual com DrawingCanvas, zonas de exclusão via ZoneTuningControls.tsx:1-196); página apps/frontend/src/pages/epi/EpiScenarioEditorPage.tsx | zonas de exclusão (exclude_zones) e perfil dia/noite só valem para tipos de operação 'epi_zone' e 'defect_trigger' (ZoneTuningControls.tsx:7-9, confirmado no backend em operations/canonical/epi_zone.py e defect_trigger.py) — outros tipos de operação não têm exclusão de zona |
| Limiares (thresholds) por câmera | ✅ sim | confidence dia/noite: apps/frontend/src/components/scenario/ZoneTuningControls.tsx:18-25,129-185 (clampConfidence, DayNightProfileInputs); confidence único: apps/frontend/src/components/scenario/ModelScenarioWizard.tsx (step 'Confiança', slider 0.10-0.99, salvo em PUT /training/scenarios/<model_id>/config); threshold por classe (model_deployments.config.thresholds): validado em geometry_validation.py:61-74 | o threshold por classe individual (model_deployments.config.thresholds) não tem tela própria localizada — só dia/noite (ZoneTuningControls) e confiança única por modelo (ModelScenarioWizard) têm UI |
| Nome/rótulo customizado da câmera | ✅ sim | services/api/app/domain/services/camera_service.py:142-150 (name obrigatório na criação) e :412 (name em campos permitidos de update); apps/frontend/src/components/cameras/CameraWizard.tsx:23,26,63 (campo name, usado tanto para criar quanto editar — cam?.name); wizard montado com camera=editingCamera em apps/frontend/src/pages/CamerasPage.tsx:418-419 | nada — nome é editável tanto na criação quanto na edição de câmera existente |
| FPS/qualidade de stream e qualidade de coleta por câmera | ✅ sim | services/api/app/api/v1/cameras/config_handler.py:96-171 (PATCH /api/cameras/<id>/config — fps_target, quality_preset, collection_subtype); apps/frontend/src/components/cameras/CameraFpsConfig.tsx:116-452 (seletor completo com painel de saúde do edge) | nada — cobre os dois eixos (operação e coleta) documentados no próprio código, incluindo propagação best-effort para o edge (config_handler.py:53-82) |
| Retenção de vídeo por câmera (tiers 1/7/30/90 dias) | ⚠️ parcial | services/api/app/api/v1/cameras/retention_handler.py:51-116 (GET/PUT /api/cameras/<id>/retention); apps/frontend/src/modules/admin/services/adminService.ts:444-447 (getCameraRetention/setCameraRetention definidos) | nenhum componente de UI chama getCameraRetention/setCameraRetention (grep não achou uso fora do próprio adminService.ts) — só a retenção do TENANT (video_retention_days) tem tela em AdminRetentionPage.tsx; a retenção POR câmera não tem tela |

**Resumo:** O "estúdio de cena" por câmera existe de fato para: nome/rótulo, FPS/qualidade de stream+coleta, atribuição de modelo por módulo (EPI/Qualidade/Contagem), vínculo câmera↔módulo, e zonas/ROI/linha/ponto com thresholds dia-noite (ScenarioEditor). Dois pontos ficam parciais: agendamento por horário (schedule_rules) e retenção por câmera têm endpoint e serviço backend prontos e testados, mas nenhuma tela no frontend os consome — API pronta, UI ausente. O registry-level model-config (roi/classes/thresholds por classe, com histórico e rollback) também não tem consumidor de frontend localizado, apesar de totalmente implementado e validado no backend.

### 3.2 · Painel ao vivo

| Item | Existe? | Onde (file:line) | O que falta |
|---|---|---|---|
| Componente de vídeo HLS (player) | ✅ sim | apps/frontend/src/components/monitoring/CameraPlayer.tsx:56-442 (hls.js, watchdog de stall linhas 16/93-98, backoff 22/106-117, recuperação de token/rede 30/125-161); usado em apps/frontend/src/components/camera-grid/CameraCell.tsx:186-194 | nada — componente completo e ativo |
| Resolução de URL de live view (token de playback) | ✅ sim | apps/frontend/src/hooks/useLiveView.ts:1-271 — POST /stream/start via cameraService, cache/dedupe (linhas 48-92), renovação proativa ancorada no exp do token (linhas 190-259) | nada |
| Pipeline de streaming — lado edge (RTSP → HLS → push) | ✅ sim | services/edge-sync-agent/app/live_view/live_view_loop.py:120-621 (FFmpeg via hls_transcoder.py, push via segment_pusher.py, ciclo tick() linhas 221-280, lifecycle on-demand por espectador 'wanted' linhas 194-219) | nada — pipeline completo edge→cloud (ADR-0020/D-74/D-85 documentados no próprio arquivo) |
| Pipeline de streaming — lado API (serve_hls/start/stop/status) | ✅ sim | services/api/app/api/v1/cameras/stream_handlers.py: start_stream (193), stop_stream (322), stream_status (339), serve_hls (425), stream_info (690) | nada |
| Overlay de bounding box | ✅ sim | apps/frontend/src/components/monitoring/DetectionOverlay.tsx:1-92 (canvas com pointerEvents:none, sem onClick — regras nas linhas 4-7; cores por classe linhas 27-40; desenho no useEffect linhas 51-82); usado em CameraCell.tsx:196-202 | nada — cumpre a regra 'pointerEvents none, zero onClick' do CLAUDE.md |
| WebSocket/SocketIO de detecção em tempo real (frontend) | ✅ sim | apps/frontend/src/hooks/useMonitoringSocket.ts:41-121 (socket.io-client, namespace /monitor linha 51, subscribe_camera/unsubscribe_camera linhas 94-107, evento 'detection' linha 74); consumido em apps/frontend/src/components/camera-grid/CameraGrid.tsx:68,145 e apps/frontend/src/app/epi/AoVivo.tsx:699-738 | nada |
| WebSocket/SocketIO de detecção (backend, emissão) | ✅ sim | services/api/app/core/socket_bridge.py:1-22 (Redis pub/sub det:* → SocketIO), linha 210 monta o evento 'detection' na room /monitor por tenant; services/api/app/core/socket_auth.py:43 registra namespaces (/monitor, /training, /quality) | nada |
| Reconexão automática do player HLS | ✅ sim | apps/frontend/src/components/monitoring/CameraPlayer.tsx: watchdog+backoff (linhas 93-117, BACKOFF_DELAYS_MS 1s/2s/5s), recuperação de erro fatal de rede/token com backoff próprio (linhas 119-161, NETWORK_RECOVERY_DELAYS_MS até 30s), reaquisição ao voltar de aba oculta (linhas 305-356) | nada — dois ciclos de reconexão distintos (stall vs. erro de rede/token), ambos sem desistência automática silenciosa |
| Reconexão automática do WebSocket de detecção | ✅ sim | apps/frontend/src/hooks/useMonitoringSocket.ts:56-59 (reconnection:true, reconnectionAttempts:Infinity, backoff 1s→10s) + re-subscribe de câmeras no evento 'connect' (linhas 62-68) | nada |
| Indicador de saúde do stream — nível ladrilho/UI ao vivo | ✅ sim | apps/frontend/src/app/epi/AoVivo.tsx:110-115 (estados online/conectando/offline derivados só do caminho real de playback — ver comentário linhas 39-45), linha 429 (cálculo do estado por câmera), linha 706 ('semSinal' quando o socket de detecção cai) | nada, mas é deliberadamente um sinal aproximado (derivado do playback, não de telemetria real do stream) — decisão consciente documentada no próprio arquivo, não lacuna |
| Indicador de saúde do stream — painel agregado (admin/observability) | ✅ sim | apps/frontend/src/modules/admin/pages/observability/StreamsPanel.tsx:1-40+ (consome GET /admin/observability/streams, badge 'Transmitindo'/'—' por câmera, substituiu N+1 de GET /stream/status por câmera) | endpoint /admin/observability/streams do lado servidor não foi lido nesta varredura — não confirmado file:line do backend |

**Resumo:** Todos os seis itens pedidos existem e estão em uso real no branch levantamento/pandora-feira-2026-09-12: player HLS com watchdog/backoff (CameraPlayer.tsx), pipeline completo edge (live_view_loop.py) → API (stream_handlers.py), overlay de bbox em canvas sem pointer events (DetectionOverlay.tsx), e WebSocket de detecção fim-a-fim (useMonitoringSocket.ts no front, socket_bridge.py no backend via Redis pub/sub). Reconexão automática existe em duas camadas independentes (hls.js watchdog+backoff e socket.io reconnection nativo). O único ponto não verificado até o fim foi o backend do painel agregado de observability (StreamsPanel consome um endpoint que não foi lido nesta sessão).

### 3.3 · Evidências

| Item | Existe? | Onde (file:line) | O que falta |
|---|---|---|---|
| Geração de clipe/frame no momento do evento (EPI/alertas) | ✅ sim | services/api/app/infrastructure/queue/tasks/inference.py:686-712 (_save_alert: cv2.imencode do frame ao vivo, evidence_key=f"evidence/{camera_id}/{timestamp}.jpg", upload via storage.upload_bytes) chamando _gravar_alerta (linhas 573-645) que grava evidence_key em AlertRepository.create; caminho do edge em alerta_de_evento_do_edge recebe evidence_r2_key já enviado pelo device (linha ~590,756-866) | É FRAME (foto), não clipe de vídeo, no caminho EPI/alertas — CLAUDE.md do repo (ADR-0033) já registra isso: "Backend de captura de clipe = roadmap" para o momento do evento de alerta; clipe de vídeo (FFmpeg ±30s) só existe implementado para o módulo Qualidade (quality_clips.py), não para alertas EPI |
| Geração de clipe no momento do evento (módulo Qualidade) | ✅ sim | services/api/app/infrastructure/queue/tasks/quality_clips.py:162-326 (generate_quality_clip: busca segmento gravado, baixa do R2, corta ±30s com ffmpeg -ss/-t, sobe clip .mp4 pra quality-clips/{tenant}/{camera_id}/{inspection_id}.mp4, atualiza clip_status) | nada relevante para este item; depende de existir quality_recording_segments com status='available' contendo o timestamp (se não achar segmento, clip_status vira 'unavailable', linhas 229-242) |
| Upload para storage R2 (Cloudflare) | ✅ sim | services/api/app/infrastructure/storage/r2_storage.py (implementação real R2Storage) + services/api/app/infrastructure/storage/local_storage.py:130-213 (factory get_storage() monta R2Storage a partir de R2_ENDPOINT/R2_KEY/R2_SECRET, com fallback para LocalStorage em dev) | nada — mecanismo de upload real e testado (preflight de conectividade em local_storage.py linhas 24-26); porém ADR-0045 mudou a política: R2 deixou de ser destino de TODO evento e virou 'upload seletivo e diferido' (dataset), condicionado por _should_upload_evidence_to_r2 em quality_clips.py:89-152 — no caminho EPI (inference.py) o upload de evidência do frame de alerta continua incondicional, não passou pelo mesmo gate |
| Deduplicação (evita clipe/evidência repetida do mesmo evento) | ⚠️ parcial | services/api/app/core/rajada.py:21 (DEDUP_WINDOW_SECONDS=60, usado só para ORDENAR fila de verificação e contar total_situacoes em AlertRepository.list_with_filters — não impede novo INSERT de alerta/evidência); services/api/app/infrastructure/queue/tasks/inference.py:437-455 (_try_acquire_auto_capture_dedup_lock, TTL 30s via Redis SET NX EX) — mas esse lock só protege a AUTO-CAPTURA de frame de treino (WS-B3), não a evidência do próprio alerta; services/api/app/api/v1/edge_events/routes.py:75-77 + edge_event_repository.py:24-32 (dedup_key = hash SHA256 do payload, ON CONFLICT DO NOTHING) — isso é idempotência de reenvio de batch, não dedup de 'mesmo fato redetectado frame a frame' | não há dedup que impeça gerar/subir uma NOVA evidência (frame ou clip) para o mesmo evento físico redetectado em frames consecutivos no caminho de alerta EPI — o próprio rajada.py documenta o achado: 423 alertas no acervo RVB, boa parte é o MESMO fato repetido (66 linhas de uma câmera em 2min = só 2 situações reais); a janela de 60s hoje só reordena/conta na fila, não suprime a criação do alerta nem do upload de evidência |
| Política de retenção (expira/apaga evidência) | ✅ sim | services/api/app/infrastructure/queue/tasks/retention_expiry.py:72-186 (expire_evidence_by_retention: retenção efetiva = camera.retention_days → tenant.default_retention_days → plan.video_retention_days → fallback 7 dias; deleta evidence_r2_key do R2 e zera a coluna, mantendo o alerta para auditoria) para EPI; services/api/app/infrastructure/queue/tasks/quality_cep.py:153-254 (cleanup_quality_recordings a cada hora por RECORDING_RETENTION_HOURS, cleanup_quality_clips diário por CLIP_RETENTION_DAYS) para Qualidade | nada estrutural — os dois jobs existem e são escopados por tenant/câmera (C-04); não foi verificado neste inventário se o Celery Beat está de fato agendando as duas tasks em produção (schedule não lido) |
| Garantia 'cloud-first' citada no CLAUDE.md (ADR-0028/0033) | ⛔ não | docs/decisions/adr/0028-cloud-first-evidence-storage.md:3 — cabeçalho explícito: 'SUPERSEDED por ADR-0045 (2026-07-14): a evidência passa a ser recorder-first (gravador do site = fonte primária; R2 = dataset seletivo)'; docs/decisions/adr/0045-evidencia-recorder-first.md:11-21 confirma a troca de política | o CLAUDE.md do repo (raiz de 'EPI - CATH V2') cita ADR-0028/0033 como se cloud-first ainda valesse — desatualizado frente ao ADR-0045, que é o que está implementado: mini-API local (services/edge-sync-agent/app/evidence_api.py, 180 linhas, task-090) + cliente ONVIF do gravador (onvif_recorder_client.py, 501 linhas, task-091) já existem no código; upload seletivo pro R2 (task-092) parcialmente feito via _should_upload_evidence_to_r2 só no módulo Qualidade, não no módulo EPI/alertas |

**Resumo:** O sistema de evidências tem dois caminhos distintos e desalinhados: alertas EPI sobem FRAME (não clipe) por evento via inference.py, sem gate de seleção nem dedup real de upload; o módulo Qualidade tem clipe de vídeo (±30s, ffmpeg) e retenção completos, e já respeita o ADR-0045 (recorder-first) via `_should_upload_evidence_to_r2`. A garantia "cloud-first" citada no CLAUDE.md (ADR-0028/0033) está tecnicamente superada desde 2026-07-14 pelo ADR-0045 (recorder-first + mini-API local no edge), que já tem implementação real em services/edge-sync-agent (evidence_api.py, onvif_recorder_client.py) — o CLAUDE.md do repo está desatualizado nesse ponto. Deduplicação de evidência repetida do mesmo evento físico existe só parcialmente (janela de 60s usada para ordenar/contar fila, TTL de 30s só para auto-captura de treino) — nenhuma trava impede novo upload de evidência a cada redetecção do mesmo fato no caminho de alertas EPI.

### 3.4 · Relatórios de conformidade

| Item | Existe? | Onde (file:line) | O que falta |
|---|---|---|---|
| Geração de relatório sob-demanda (compliance EPI) | ✅ sim | services/api/app/api/v1/reports/routes.py:35-93 (GET /api/reports/compliance) chama services/api/app/domain/services/compliance_report_service.py:284-338 (ComplianceReportService.generate) | nada — aceita period=dia\|semana ou from/to customizado (routes.py:54-78) |
| Geração de relatório periódico agendado (job automático) | ✅ sim | services/api/app/infrastructure/queue/tasks/compliance.py:50-88 (generate_daily_compliance_reports), agendado em services/api/app/infrastructure/queue/celery_app.py:83-87 (schedule=86400, fila 'reports') | só cobre period='dia' e só tenants com módulo EPI habilitado (compliance.py:37-47); não há job agendado para period='semana' |
| Contagem de eventos por período | ✅ sim | compliance_report_service.py:378 (alert_repo.count_in_window) e :481 (chave total_violations no retorno); consumido em apps/frontend/src/app/epi/Relatorios.tsx:528 | nada funcionalmente, mas o nome da chave engana: total_violations conta TODO alerta EPI (violação E conformidade), não só violação — documentado como dívida deliberada em compliance_report_service.py:473-481 |
| EPIs faltantes por câmera (breakdown por câmera) | ⚠️ parcial | compliance_report_service.py:390-410 monta top_cameras via alert_repo.list_with_filters, contando por camera_id (Counter), exposto em summary.top_cameras | top_cameras conta TODOS os eventos por câmera (violação + conformidade, ver EIXO_TOP_CAMERAS em compliance_report_service.py:157), não especificamente EPI faltante; o repositório tem violations_by_class (alert_repository.py:1054-1097, filtra só violação de verdade) mas essa função NÃO é chamada por compliance_report_service — não há endpoint que cruze câmera × classe de EPI faltante |
| EPIs faltantes por período (breakdown temporal) | ⚠️ parcial | compliance_report_service.py:381-388 usa alert_repo.count_by_hour para trend_by_hour | trend_by_hour é contagem de eventos por hora sem filtrar por classe de EPI nem por tipo violação/conformidade — não há série temporal específica de 'EPI faltante' |
| Exportação em PDF | ✅ sim | compliance_report_service.py:192-278 (_generate_pdf, reportlab) chamado em generate() linha 318; upload no R2 em :320-328; frontend baixa via Relatorios.tsx:248-254 | nada — inclui tabela de sumário e top câmeras |
| Exportação em CSV | ✅ sim | services/api/app/api/v1/alerts/routes.py:175-218 (GET /api/alerts/export); consumido em Relatorios.tsx:100 (ROTA_CSV) e :255-261 | é endpoint separado do /reports/compliance — exporta linhas de alerta cru (data/câmera/classe/confiança/reconhecido), não o mesmo sumário agregado do PDF; forma fixa, sem seleção de colunas (documentado em Relatorios.tsx:21-26) |
| Agendamento de envio do relatório ao cliente (ex.: e-mail) | ⛔ não | não encontrado — busca por 'digest'/e-mail agendado em services/api/app não retornou implementação; único achado é o comentário em apps/frontend/src/app/epi/Relatorios.tsx:13-19 registrando a ausência explicitamente | todo o fluxo de envio: endpoint (o front cita /api/v1/notifications/channels como canal genérico sem envio real), configuração de destinatários/horário, disparo de e-mail com o PDF anexado ou link; hoje o job agendado (compliance.py) só ARQUIVA o PDF no R2, ninguém é notificado |

**Resumo:** O backend tem um serviço de relatório de compliance EPI completo e funcional: endpoint sob-demanda (GET /api/reports/compliance), job diário agendado via Celery Beat que gera e arquiva PDF no R2, e exportação CSV de alertas via endpoint separado (/api/alerts/export) — tudo tenant-scoped e coberto no frontend (Relatorios.tsx). Os breakdowns "por câmera" e "por período" existem mas contam eventos totais (violação+conformidade), não especificamente EPI faltante — o repositório tem violations_by_class capaz disso mas não é usado no relatório de compliance. O único item totalmente ausente é o agendamento de ENVIO (e-mail/digest) ao cliente — o próprio código-fonte (Relatorios.tsx) documenta essa lacuna explicitamente, dizendo que não existe endpoint de envio.

### 3.5 · Onboarding de câmera (passo a passo até detectar)

| Item | Existe? | Onde (file:line) | O que falta |
|---|---|---|---|
| 1. Criação do site edge (pré-requisito, 1x por site, não por câmera) | ✅ sim | services/api/app/api/v1/edge/routes.py:1408-1435 (create_site); tabela edge_sites | nada — é passo humano via painel, fora do wizard de câmera |
| 2. Geração de enrollment token do device (admin, one-time, 24h) | ✅ sim | services/api/app/api/v1/edge/routes.py:1547-1588 (create_enrollment_token); token hash SHA-256 em enrollment_tokens.token_hash | nada |
| 3. Enrollment do device edge (Jetson gera par de chaves, resgata token, auto-assina JWT RS256) | ✅ sim | services/api/app/api/v1/edge/routes.py:1668-1716 (enroll_device); verificação em app/core/device_auth.py:82-100 (verify_device_token). Fluxo real diverge do texto original da ADR — confirmado em docs/decisions/adr/0019-device-tokens-rs256.md:87-97 (reconciliação 2026-07-18): device auto-assina, cloud nunca tem chave privada de device | rotação de chave sem re-enrollment não existe (ADR-0019 linha 99: /api/v1/edge/auth/rotate não existe) |
| 4. Fluxo alternativo de claim-code (devices_bp) para enrollment | ⚠️ parcial | services/api/app/api/v1/devices/routes.py:47-151 (create_claim_code, claim_device) gera um segundo tipo de 'enrollment_token' (JWT com token_type=device_enrollment) via app/core/device_auth.py:52-56 | esse token não é consumido em nenhum lugar do código (grep confirma generate_enrollment_token só é chamado em devices/routes.py:133) — não bate com o hash opaco que POST /api/v1/edge/enroll espera (tabela enrollment_tokens, diferente de device_claim_repository). É uma via de enrollment paralela e desconectada do caminho realmente usado pelo edge |
| 5. Probe de conectividade da câmera (RTSP/ffprobe) antes de salvar | ✅ sim | services/api/app/api/v1/cameras/probe_handler.py:171-289 (probe_camera, POST /api/cameras/probe); UI: apps/frontend/src/components/cameras/CameraOnboardingWizard.tsx:58-86 (step 'Verificação') | descoberta automática de câmeras na rede (scan/discovery ONVIF) não existe — grep por 'discover' no backend não retornou nada; ONVIF no repo serve só GetSnapshotUri (D-85, snapshot_handlers.py) e o cliente de NVR Profile G (services/api/app/infrastructure/nvr/onvif_client.py:1-16, que o próprio arquivo documenta como não testado contra hardware real) |
| 6. Criação do registro da câmera | ✅ sim | services/api/app/api/v1/cameras/crud_handlers.py:112-151 (create_camera, POST /api/cameras); UI: CameraOnboardingWizard.tsx:88-116 (saveCamera) | o payload enviado pelo wizard (linhas 92-105) não inclui site_id |
| 7. Associação câmera → site_id (necessária para o edge enxergar a câmera) | ⚠️ parcial | backend aceita via PUT /api/cameras/<id> (crud_handlers.py:181-219, campo site_id); persistência em services/api/app/infrastructure/database/repositories/camera_repository.py:64 (site_id vira NULL se ausente). O próprio docstring do endpoint (crud_handlers.py:128-130) avisa: 'SEM ele a câmera fica invisível pro edge (config_poll, live view, fps demand filtram por site_id)' | NENHUMA tela do frontend escreve site_id. Confirmado por grep em todo apps/frontend/src: site_id só é LIDO para exibição (apps/frontend/src/app/epi/Cameras.tsx:607,626,793,1225,1239,1356); CameraWizard.tsx, CameraOnboardingWizard.tsx e CameraCard.tsx não têm nenhum campo de seleção de site. É um gap real de onboarding via UI — hoje só dá para setar via chamada direta à API/seed |
| 8. Vínculo câmera ↔ módulo (tabela camera_modules, migration 134 — 'para que serve a câmera') | ✅ sim | services/api/app/api/v1/cameras/modules_handler.py:87-199 (list_camera_modules / put_camera_modules, GET+PUT /api/cameras/modules) | nada; UI referenciada em apps/frontend/src/app/estudio/CamerasPorModulo.tsx (não lida linha-a-linha nesta sessão) |
| 9. Módulo ativo da câmera (cameras.active_module, coluna 1:1 lida pelo worker) | ✅ sim | services/api/app/api/v1/cameras/module_handler.py (patch_camera_module, PATCH /api/cameras/<id>/module — registrado em routes.py) | nada observado |
| 10. Atribuição de modelo treinado à câmera (ou herança do manifesto do módulo) | ✅ sim | services/api/app/api/v1/cameras/model_handlers.py:136-202 (set_camera_model) e 237-305 (put_camera_models); fallback de herança em get_effective_model:352-396 (usa ModelRolloutRepository quando não há override) | atribuição explícita falha com 400 se o passo 8 (vínculo camera_modules) não foi feito antes — _check_camera_module, model_handlers.py:78-95 |
| 11. Device edge baixa a configuração (câmeras do site, regras, cenário, manifesto de modelo) | ✅ sim | services/api/app/api/v1/edge/routes.py:615-651 (_build_edge_config_payload) e 762-830 (poll_edge_config, GET /api/v1/edge/config/poll) | manifesto de modelo só é incluído se trained_models.metrics.onnx_sha256 já foi gravado pela task de validação — sem esse digest, o payload sai SEM a chave 'model', silenciosamente (_build_model_manifest, routes.py:684-745, com log INFO em vez de erro visível na tela) |
| 12. Humano inicia o monitoramento/stream da câmera | ✅ sim | services/api/app/api/v1/cameras/stream_handlers.py (start_stream, POST /api/cameras/<id>/stream/start); frontend: apps/frontend/src/services/cameraService.ts:229, botão em apps/frontend/src/app/epi/Cameras.tsx:1261 | decide edge-fed vs FFmpeg local via _edge_fed_by_config (stream_handlers.py:63-93), que depende do site_id da câmera — se o passo 7 não foi feito, cai no caminho de FFmpeg local, que nunca conecta numa câmera atrás de VLAN isolada (ADR-0020) |
| 13. Inferência no edge (DeepStream) e propagação da detecção até o browser | ⚠️ parcial | estrutura de diretórios confirma a existência (deepstream/, services/edge-sync-agent/) mas o caminho linha-a-linha não foi lido nesta sessão — fora do escopo services/api examinado | verificação file:line do pipeline Redis pub/sub detections:{camera_id} → edge-sync-agent → API → SocketIO não foi feita nesta varredura; não afirmo além do que a estrutura de arquivos mostra |

**Resumo:** Contei 13 passos distintos do onboarding real de uma câmera nova (2 deles pré-requisitos de site/device, não por câmera): criação de site → geração e resgate de enrollment token (RS256, device auto-assina — ADR-0019 diverge do texto original, confirmado em código) → probe RTSP → criação da câmera → vínculo a site/módulo/modelo → poll de config pelo edge → início manual do stream → inferência no edge. Não existe descoberta automática ONVIF de câmeras (ONVIF no repo só serve NVR Profile G e GetSnapshotUri). O gap mais sério, confirmado por grep completo do frontend: nenhuma tela escreve `site_id` na câmera — o próprio backend documenta que sem isso ela fica invisível ao edge — e há uma segunda via de enrollment (claim-code, devices_bp) que gera um token que nenhum endpoint consome, desconectada do fluxo real usado pelo edge.

### 3.6 · Autenticação

| Item | Existe? | Onde (file:line) | O que falta |
|---|---|---|---|
| Login usuário (JWT HS256) + claims tenant_id/tenant_schema/role/modules_enabled | ✅ sim | services/api/app/api/v1/auth/routes.py:135-201 (POST /login), :398-474 (_issue_session_token monta additional_claims tenant_id/tenant_schema/email/role/modules/perms); services/api/app/core/auth.py:87-131 (get_tenant_id/get_role/get_tenant_schema/get_modules_enabled leem as claims, raise AuthenticationError se ausentes — ADR-0017); services/api/app/config.py:18-21 (JWT_ALGORITHM='HS256', JWT_EXPIRY_HOURS default 24); services/api/app/__init__.py:83-89 (config aplicada ao Flask-JWT-Extended) | Nada essencial — fluxo completo e único ponto de emissão (_issue_session_token compartilhado por /login e /refresh). |
| Autenticação de device edge — JWT RS256 com escopos (ADR-0019) | ✅ sim | services/api/app/core/device_auth.py:62-154 (extract_device_id_unverified, verify_device_token com algorithms=['RS256'], authenticate_device faz lookup+revoked-check+verify+match de claims); services/api/app/core/device_auth.py:177-212 (require_device_scope — decorator deny-by-default por escopo, seta flask.g.device_ctx); services/api/app/api/v1/edge/routes.py:1668-1716 (POST /enroll — consome enrollment_token, persiste public_key_pem+fingerprint); services/api/app/api/v1/edge/routes.py:511-599 (POST /heartbeat replica manualmente o mesmo fluxo de authenticate_device, sem usar o decorator); services/api/app/infrastructure/database/repositories/edge_site_repository.py:197-247 (INSERT/upsert de public_key_pem) | Nada crítico — device_auth.py documenta explicitamente que o caller deve checar revoked antes de verify_device_token, e authenticate_device/require_device_scope fazem isso corretamente. Observação menor: /heartbeat (edge/routes.py:511-599) duplica manualmente a lógica de authenticate_device() em vez de reutilizá-la — não é falta funcional, é duplicação de código a considerar em refactor. |
| Rotação/expiração de token (usuário) | ✅ sim | services/api/app/api/v1/auth/routes.py:496-574 (POST /refresh — @jwt_required(), rejeita claims tenant_ctx/imp/token_type via _CLAIMS_NAO_RENOVAVEIS:493, relê user do banco, bloqueia se force_password_reset ou is_active=false, reemite via _issue_session_token); services/api/app/domain/services/session_service.py (single_session: revoke_other_sessions + blocklist_jtis grava revoked_jti:<jti> no Redis com TTL = tempo restante); services/api/app/__init__.py:538-556 (token_in_blocklist_loader consulta is_jti_revoked em toda request — fail-open se Redis indisponível, documentado) | GAP arquitetural documentado no próprio código (session_service.py:15-22): não existem refresh tokens de fato — access token vive JWT_EXPIRY_HOURS (24h default) e a revogação depende inteiramente do Redis (fail-open). Mitigação futura citada no próprio TODO: access tokens curtos (~15min) + refresh token, ou checagem em active_sessions com cache. Além disso, há uma função morta _register_session_blocklist (services/api/app/__init__.py:519-535) que nunca é chamada — só _register_jwt_error_handlers (chamada em :125) registra o loader real; não é um bug funcional (o loader ativo funciona), mas é código duplicado/não invocado que pode confundir leitura futura. |
| Rotação/expiração de token (device edge) | ✅ sim | services/api/app/core/device_auth.py:83-105 (verify_device_token com options verify_exp=True — expiração pura via exp do JWT RS256, sem blocklist); services/api/app/api/v1/edge/routes.py:1627-1658 (POST /enrollment-tokens/<id>/revoke — revoga token de enrollment não-usado) e :1858-1888 (POST /devices/<id>/revoke — revoga device ativo, corta acesso imediato pois authenticate_device checa device['revoked'] antes de verificar assinatura) | Não há rotação de chave RSA do device nem TTL de expiração no próprio token de sessão do device (o RS256 do device não tem 'exp' companheiro de refresh — expira e o device fica sem sessão nova, precisando reenroll?). Não encontrado no código lido um endpoint de renovação de token de device (equivalente ao /refresh de usuário) — se existir, está fora dos arquivos varridos (edge/routes.py, device_auth.py, devices/routes.py); resultado negativo explícito: NÃO ENCONTRADO fluxo de refresh de device token nestes arquivos. |
| Superadmin — gates de autorização | ✅ sim | services/api/app/core/tenant.py:87-97 (require_superadmin — 403 AuthorizationError se role != 'superadmin'); services/api/app/core/tenant_context.py:87-104 (require_superadmin_or_404 — mesmo gate mas retorna 404 em vez de 403, para não vazar existência do recurso, C-01); services/api/app/core/tenant.py:114+ (has_permission — 'superadmin' sempre True, bypass de permissão, comentado como 'imune a deny — anti-lockout') | Nada — dois gates distintos (403 vs 404) documentados e usados propositalmente em contextos diferentes. |
| Superadmin — assumir contexto de tenant | ✅ sim | services/api/app/api/v1/admin/tenant_context_routes.py:130-223 (POST /tenants/<id>/assume — @require_superadmin_or_404, nega aninhamento se claim tenant_ctx ou imp já presentes, emite token com sub=próprio superadmin + tenant_id/tenant_schema/modules do alvo + claim tenant_ctx=True, TTL=TENANT_CONTEXT_TTL_MINUTES); services/api/app/core/tenant_context.py:69 (TTL default 30min, env-overridável); :225-296 (POST /renew — só renova token que já tem tenant_ctx=True); services/api/app/core/tenant_context.py:96-140 (register_tenant_context_audit — after_request grava toda requisição feita sob claim tenant_ctx em public.tenant_context_audit, migration 108) | Nada essencial — fluxo completo com auditoria por requisição e proteção contra aninhamento com impersonation. |
| Superadmin — impersonação ('ver como' usuário) | ✅ sim | services/api/app/api/v1/admin/impersonation_routes.py:106-215 (POST /users/<id>/impersonate — @require_superadmin, nega aninhamento se claim 'imp' já presente, nega alvo superadmin/inativo/sem tenant, token com identity=ALVO + claim imp=True/impersonated_by/impersonator_email, TTL=IMPERSONATION_TTL_MINUTES=30 fixo); :218-245 (POST /stop — encerra por jti, best-effort); tabela public.impersonation_sessions (migration 086, citada no docstring) | Nada essencial — TTL fixo (30min, não env-overridável ao contrário do tenant_context) é uma diferença de design documentada, não uma falta. |

**Resumo:** Autenticação de usuário (JWT HS256 com tenant_id/tenant_schema/role/modules), device edge (JWT RS256 com escopos, ADR-0019), rotação/expiração e os dois modos de "virar superadmin de outro contexto" (assumir tenant e impersonar usuário) existem todos e estão implementados em services/api/app/core/{auth,device_auth,tenant,tenant_context}.py e nas rotas correspondentes em app/api/v1/{auth,admin,devices,edge}. O único gap real é arquitetural e já documentado no próprio código: não há refresh token verdadeiro para sessão de usuário — a revogação depende só de blocklist Redis fail-open (session_service.py). Não encontrei, nos arquivos varridos, um endpoint de renovação de token para device edge equivalente ao /refresh de usuário.

### 3.7 · Dependência de nuvem no caminho (o que trava o fork 100% offline)

| Item | Existe? | Onde (file:line) | O que falta |
|---|---|---|---|
| Enrollment do device (POST /api/v1/edge/enroll) — identidade RS256 inicial | ✅ sim | services/edge-sync-agent/app/auth/enrollment.py:59-138 (POST em :100, retry/backoff :95-138); disparado em services/edge-sync-agent/app/main.py:373-377 (EnrollmentError -> sys.exit(1)) | BLOQUEANTE, mas só no PRIMEIRO boot de um device sem identity.json persistido (enrollment.py:74-79 pula a rede se já há identidade válida). Bloqueia apenas o processo edge-sync-agent (main.py:run_daemon) — não afeta DeepStream/publicador, que são serviços systemd separados. Para a feira: pré-enrolar o device com internet antes, ou copiar um identity.json/chave já enrolados para o box offline. |
| Bearer/token do device para as chamadas seguintes — assinado localmente (RS256), sem chamada de rede | ⛔ não | services/edge-sync-agent/app/auth/token_manager.py:212-234 (get_bearer() só usa a chave privada local, jwt.encode local; nenhum POST/GET encontrado neste arquivo) | Nada — resultado negativo confirmado: depois do enroll, a nuvem nunca reemite token (comentário em heartbeat.py:9-10). Não é dependência de nuvem; citado para provar que a autenticação corrente não é um ponto de bloqueio. |
| ConfigPoller — GET /api/v1/edge/config/poll (câmeras/regras/cenário/manifest de modelo), a cada 45s | ✅ sim | services/edge-sync-agent/app/config_poller.py:53 (URL), :148-156 (GET), :236-240 (loop) | DEGRADA, não bloqueia. Em erro/timeout (:168-170) mantém em memória a última config boa — câmeras/regras não somem. Sem nuvem, o site nunca recebe config NOVA (nem troca de modelo), mas continua com o que já tinha ao subir. |
| CommandPoller — GET /api/v1/edge/commands/pending + PATCH /commands/<id> (ack) | ✅ sim | services/edge-sync-agent/app/command_poller.py:135-136 (URLs), :161-175 (GET), :374-387 (_ack PATCH) | DEGRADA. Falha de rede só loga e devolve 0 comandos tratados (:173-175); não derruba nada. Sem nuvem, comandos remotos (capture_snapshot, run_propagation, monitoring.*, update_camera_config) simplesmente não chegam — funcionalidades de controle remoto ficam indisponíveis, mas a inferência/alerta local não depende deste poller. |
| Uploader — POST /api/v1/edge/events/ingest (envio dos eventos/alertas em lote) | ✅ sim | services/edge-sync-agent/app/uploader.py:86 (URL), :108-135 (_try_upload, POST em :113) | DEGRADA. Buffer SQLite (sqlite_buffer.py) acumula localmente (mark_failed em :133) e tenta de novo com backoff 30s→300s; nada se perde e a detecção local não é afetada. Sem internet permanente na feira, o buffer só cresce sem drenar — revisar se há teto de retenção caso o offline seja mais que demonstração. |
| HeartbeatLoop — POST /api/v1/edge/heartbeat (telemetria de liveness do box) | ✅ sim | services/edge-sync-agent/app/heartbeat.py:118 (URL), :154-201 (send_once, POST em :169) | DEGRADA. Erro de rede só faz backoff (:175-180); 403 marca o device como revogado e para SÓ este loop (:194-197, DeviceRevokedError). Sem nuvem, a frota simplesmente não vê o box como vivo — zero impacto na detecção. |
| Evidência do alerta — SnapshotExecutor.capture_evidence, POST /api/v1/edge/cameras/<id>/evidence (JPEG do frame que motivou o alerta) | ✅ sim | services/edge-sync-agent/app/detection_relay.py:180-224 (chamada a evidence_capture, teto 20/min em :140-150, pausa 60s em :217-224); services/edge-sync-agent/app/snapshot_executor.py:221-251 (_upload, POST), :255-310 (capture_evidence, 'Nunca levanta') | DEGRADA por desenho explícito ('alerta sem imagem é ruim; alerta que não chega é pior', snapshot_executor.py:277). Sem nuvem, o evento sobe ao buffer igual, só sem evidence_r2_key. Ressalva: o destino final da imagem é o Cloudflare R2 ('cloud-first', CLAUDE.md) — fora do escopo services/api auditado aqui — então mesmo apontando o edge-sync-agent para uma API local, a foto do alerta continuaria dependendo de rede externa até essa camada de storage ser revisada. |
| capture_snapshot (comando manual de miniatura) — mesmo SnapshotExecutor, mas disparado por comando da nuvem | ✅ sim | services/edge-sync-agent/app/command_poller.py:247-278 (_handle_capture_snapshot); snapshot_executor.py:114-164 (capture_and_upload) | DEGRADA/inerte sem nuvem: como depende do CommandPoller para sequer chegar o comando (item acima), sem internet essa função nunca é acionada — não é um bloqueio, é uma feature de triagem remota indisponível. |
| Live view (HLS) — GET /api/v1/edge/live-view/wanted + POST /live-view/<id>/segment | ✅ sim | services/edge-sync-agent/app/live_view/segment_pusher.py:35-58 (fetch_wanted_cameras), :61-101 (push_segment); live_view/live_view_loop.py:569 (EDGE_API_URL) | DEGRADA, mas é processo separado (`python -m app.live_view`) e NÃO aparece na lista de units habilitadas por deployments/edge/install_edge_stack.sh (só recognition-api/edge-sync/edge-telemetry/deepstream@epi,parking/soak-sampler). Sem nuvem, o transcode HLS por câmera simplesmente para (SegmentPushError propaga no loop) — não toca em detecção/alerta. Se a feira quiser essa tela ao vivo funcionando 100% offline, hoje não há visualizador local — o consumo do HLS é do lado da API/nuvem. |
| Coleta de frames de treino (flywheel) — POST /api/v1/edge/frames | ✅ sim | services/edge-sync-agent/app/collector/frame_uploader.py:32-79 (upload_frame, POST em :62); collector/collector_loop.py:364-387 (EDGE_API_URL) | DEGRADA (FrameUploadError é levantado e tratado pelo loop, não derruba o processo). Processo separado (`python -m app.collector`), também ausente da lista de units habilitadas — não roda por padrão na feira. É pipeline de anotação/treino, não de detecção ao vivo. |
| replay_miner.py — mineração offline de frames via EDGE_API_URL (mesma família do coletor de treino) | ✅ sim | services/edge-sync-agent/app/collector/replay_miner.py:1342 (api_base_url=env['EDGE_API_URL']) | DEGRADA/mesmo caso do item anterior: script de mineração, não faz parte do caminho de detecção/alerta ao vivo, precisa ser disparado manualmente. |
| OTA — GET /api/v1/edge/software/target (verifica se há release nova para aplicar) | ✅ sim | services/edge-sync-agent/app/ota/client.py:22-45 (fetch_target_ref); services/edge-sync-agent/app/ota/updater.py:1-16 (roda como unit systemd SEPARADA do daemon) | DEGRADA: cloud só INDICA um alvo, nunca aplica sozinha (client.py:3); sem rede, TargetFetchError é levantado e nenhuma atualização acontece — comportamento seguro. Nenhuma unit de OTA aparece habilitada em install_edge_stack.sh, então nem roda por padrão na feira. |
| Telemetry heartbeat sink (opcional) — POST /api/v1/edge/heartbeat via `requests` | ✅ sim | services/edge-sync-agent/app/telemetry/__main__.py:39-52 (_build_heartbeat_sink, off sem EDGE_API_URL+EDGE_DEVICE_BEARER), :57 (endpoint) | DEGRADA e é opt-in: sem as duas envs, o sink simplesmente não é criado (:44-46) e tudo grava só no JSONL local (recognition-edge-telemetry.service RODA habilitado por padrão, mas o sink de rede fica desligado salvo configuração explícita). |
| Pipeline de detecção em si — DeepStream (nvinfer) + publicador KITTI→Redis + Redis local | ⛔ não | deepstream/epi/app_epi.txt:sink0 (type=1 = fakesink local, sem RTMP/rede), primary-gie aponta só para engine/label locais em /home/pandora/recognition/models/; deployments/edge/publicar_deteccoes.py:582,651 (--redis-url default 127.0.0.1:6379, único I/O de rede é redis.Redis.from_url local) | Nada — resultado negativo confirmado por leitura: nenhuma chamada HTTP/externa em nenhum dos dois arquivos. Roda em units systemd distintas (recognition-infer@/recognition-deteccoes@) da recognition-edge-sync — é a prova de que detecção+alerta local independem de todos os itens de nuvem acima. |
| Gap de configuração: EDGE_API_URL (nome lido pelo código) vs CLOUD_API_BASE (nome no edge.env.example) e fallback hardcoded para Railway real | ⚠️ parcial | deployments/edge/edge.env.example:14-15 (CLOUD_API_BASE=https://api-v3-production-2b22.up.railway.app); services/edge-sync-agent/app/auth/enrollment.py:24 e app/heartbeat.py:41 (_DEFAULT_API_URL=https://api-v3-desenvolvimento.up.railway.app, usado quando EDGE_API_URL não está setado); deployments/edge/systemd/recognition-api.service (API co-residente sobe em 0.0.0.0:$PORT, railway_start.py:25,198, porta default 8080) | Este é o item que destrava o fork 100% local: hoje, se ninguém setar EDGE_API_URL explicitamente, TODOS os itens 1,3-9,12,13 acima saem para a internet real (produção ou DEV da Railway), mesmo com recognition-api.service rodando co-residente no mesmo Jetson. Falta redefinir EDGE_API_URL=http://127.0.0.1:8080 (ou a porta configurada) no /etc/recognition/edge.env, e ajustar/renomear a variável no edge.env.example (que hoje usa um nome — CLOUD_API_BASE — que o código não lê). |

**Resumo:** Nenhuma dependência de nuvem é BLOQUEANTE para a detecção em si: DeepStream (inferência) + publicador KITTI→Redis + Redis são 100% locais (deepstream/epi/app_epi.txt, deployments/edge/publicar_deteccoes.py), e todas as 8 chamadas HTTP do edge-sync-agent (enroll, config, comandos, upload de eventos, heartbeat, evidência/snapshot, live-view, coleta de frames, OTA) degradam com backoff/buffer local sem derrubar o site — a única exceção é o enrollment, que bloqueia apenas o processo edge-sync-agent (não a inferência) e só no primeiro boot sem identidade já persistida. O que de fato destrava o fork 100% offline é redirecionar EDGE_API_URL (hoje sem default local, caindo em URLs reais da Railway) para o recognition-api.service já co-residente no próprio Jetson, e depois resolver a dependência downstream do Cloudflare R2 para a foto de evidência (fora do escopo services/api desta auditoria).

---

## ⚠️ Achado colateral, não resolvido — sha256 do modelo servido

`docs/decisions/adr/0071-o-validador-tem-tres-respostas-nao-duas.md:23` (datado de **09/09**, o mesmo dia do hotpatch/rollback encontrado no Bloco 0) registra: *"as duas sentenças de campo virgem do projeto mediram o `46a30ed9`, que **nunca esteve no box** (provado por sha256: o arquivo servido é o `8b3bd146`)"*.

Tentei confirmar se isso ainda vale **hoje**, no box: computei o sha256 do `model_46a30ed9.onnx` atual (`7f889348...`) e do seu backup de rollback (`448469e8...`). ⛔ **Nenhum dos dois bate como prefixo com `46a30ed9` nem com `8b3bd146`** — ou seja, os identificadores curtos do ADR não são o sha256 do arquivo `.onnx` em si; são algum outro identificador (versão de treino, chave R2, ou hash de outro artefato) que eu não reconstruí nesta rodada.

⛔ **Não afirmo que o achado do ADR-0071 está resolvido, nem que continua aberto.** Isso exige o mesmo método de verificação que gerou o achado original (que não está descrito no ADR), e há uma área "faixa:modelo" já ativa com múltiplos issues abertos (#542, #781, #850, entre outros) tratando problemas adjacentes de rotulagem/procedência do modelo servido. Não abri issue nova para não duplicar uma frente que já parece ter dono.

---

## Bloco 4 · Estado do modelo de fogo

**Fonte:** repos `FireSet` (pipeline de treino) e `fire-demo` (demo pública), ambos existentes e ativos — clonados read-only para `/tmp/`. Estado datado de **2026-09-09** (3 dias atrás), conforme `docs/ESTADO.md` do próprio FireSet.

### Datasets (26 mantidos de 40 auditadas — licença de cada um, sem agregar)

Todas as 26 fontes **mantidas** têm licença no allowlist permissivo (`Apache-2.0`, `BSD-2/3-Clause`, `CC-BY-4.0`, `CC0-1.0`, `MIT`) — confirmado em `audit/REPORT.md`, gerado por `fireset/audit.py` em 2026-09-08. As 14 **excluídas** o foram majoritariamente por licença NÃO-DECLARADA fora do allowlist, ou por excesso de caixas degeneradas (ex.: `avian-ag-sd77w__spark-detector`, 24,29% de caixas degeneradas). Amostra das mantidas: `pyro_sdis` (Apache-2.0, 15.000 img), `firedetection-sserj__fire_detection-uhbdr` (CC-BY-4.0, 100.004 img, maior fonte), `computer-vision-mkpxu__...` (MIT), `furg` (CC0-1.0). Lista completa das 40 com veredito, imagens, caixas e licença: `audit/REPORT.md` no FireSet.

### Classes treinadas e contagem **por split** (nunca agregado)

Split de **teste** (`EVAL.md`, 7.145 imagens, modelo `fireset_yolox_s.onnx`):

| Classe | Caixas no split de teste | AP50 | Situação |
|---|---:|---:|---|
| fire | 8.709 | 0,6622 | avaliada |
| smoke | 6.603 | 0,5945 | avaliada |
| spark | 71 | — | **INAVALIÁVEL** (n < 100) |
| arc | 26 | — | **INAVALIÁVEL** (n < 100) |
| steam | 26 | — | **INAVALIÁVEL** (n < 100) |

⛔ Contagem de **train/val** por classe não foi levantada nesta rodada — resultado negativo explícito, não uma tentativa de inferir do agregado.

### Pesos e formato

`fireset_yolox_s.onnx` (YOLOX-s, **Apache-2.0**, ZERO ultralytics/AGPL — confirmado no README do `fire-demo`), exportado com paridade conclusiva contra o `.pth` de treino (453 = 453 detecções, `fireset_yolox_s.paridade.json`). Upload para R2 confirmado em `white-vision/fireset_v1/` (guarda de prefixo, per `docs/ESTADO.md`).

### Último treino

`runs/v1-20260908T232432Z-secure/`, **DONE em 2026-09-09T09:27:43Z**. 10h00 de treino, parada por relógio (não por convergência) na época 47 de 60 alvo. `best_ap` (proxy de treino) = 0,3287. **mAP50 real (split teste, 2 classes avaliáveis) = 0,6283.**

### Deploy no Jetson (relevante para o Pandora da feira)

`docs/ESTADO.md` do FireSet, seção "Não feito, e por quê": *"`deploy/README.md` com `trtexec` para JetPack 6 — o alvo Jetson foi **dispensado pelo Vitor** nesta rodada (não existe placa ainda); ONNX em x86/GPU basta."* ⚠️ **Consequência direta para esta rodada:** hoje **não existe nenhum artefato de deploy Jetson para o modelo de fogo** — nem `.engine`, nem parser bbox customizado, nem config nvinfer. O que existe é o `.onnx` (portável) e a validação em x86/GPU. Se o Pandora da feira for rodar fogo, o trabalho de portar para Jetson (engine + parser, mesmo padrão do RF-DETR do Bloco 2) ainda não começou.

### 🔴 Veredito do próprio FireSet — v1 não serve para uma planta

`docs/ESTADO.md`, textual: *"A v1 não serve para uma planta ainda [...] a métrica de aceitação do briefing é ≤ 1 falso alarme/hora e **nenhum limiar testado a cumpre**."* Isso está publicado no `fire-demo` ao vivo, com o motivo explícito — não é um resultado escondido.

✅ **Se não existisse runner/modelo pronto, essa seria a resposta — mas existe, e o próprio time documentou por que ele não está pronto para produção.** Resultado negativo explícito e já registrado por quem fez o trabalho, não uma lacuna desta rodada.


---

## Issues abertas nesta rodada

| # | Título | Label | Bloco |
|---|---|---|---|
| [#926](https://github.com/logikos33/Recognition/issues/926) | Runner de produção do cenário epi vivia só no box, sem versionamento | debt | 0 |
| [#928](https://github.com/logikos33/Recognition/issues/928) | `publicar_deteccoes.py`/`gerar_config_deepstream.py` divergem do box (hotpatch 09/09) | debt | 0 |
| [#929](https://github.com/logikos33/Recognition/issues/929) | `source17` sem `camera_id` em `fontes_epi.json` (18 fontes, 17 mapeadas) | debt | 0/1 |
| [#930](https://github.com/logikos33/Recognition/issues/930) | `EDGE_API_URL` sem default local — tudo sai pra Railway real sem configurar | debt | 3 |
| [#931](https://github.com/logikos33/Recognition/issues/931) | Onboarding: nenhuma tela escreve `site_id` — câmera fica invisível pro edge | bug | 3 |

Não existe label `orin` no repositório (só `bug` e `debt`) — usei `debt` para os achados de infraestrutura/provisionamento.

---

## HANDOFF

### Provado

- **Bloco 0** — runner de produção do cenário `epi` versionado (PR #927, 19/20 checks verdes, `Secret detection (gitleaks)` passou — RTSP confirmadamente redigido). Achado de maior severidade: parser bbox RF-DETR só existia como binário no box, agora com fonte versionada.
- **Bloco 1** — 18 câmeras, stream principal (não substream), 720p, GPU médio 29,9%/pico 98%, CPU 11%, RAM 33,5% — medido por amostra passiva, sem carga sintética.
- **Bloco 2** — RVB já está em JetPack 6.2/DeepStream 7.1, mesma versão-alvo da feira. Engine e parser custom precisam ser regerados no Pandora novo (esperado, TensorRT não é portável); o `.onnx` e a fonte do parser, sim, são portáveis e agora estão versionados.
- **Bloco 3** — 7 subsistemas inventariados com file:line real (34 `sim`, 6 `parcial`, 2 `nao` entre 42 itens). Achado que importa mais para a feira: `EDGE_API_URL` sem default local (#930).
- **Bloco 4** — FireSet/fire-demo existem, ativos, com estado de 3 dias atrás. Modelo de fogo (YOLOX-s, Apache-2.0) treinado, **não** aprovado para produção (falso-alarme/hora não cumprido em nenhum limiar) e **sem** artefato de deploy Jetson.

### O que ficou

1. **PR #927 aguardando review/merge** — `Tests (pytest)` ainda rodando no momento deste relatório; conferir antes de mergear.
2. **#928** (drift dos scripts Python) precisa de decisão humana: portar o hotfix do box ou descartar.
3. **A dúvida do sha256 do modelo `46a30ed9`/`8b3bd146`** (ADR-0071) não foi fechada — nem reaberta, nem confirmada resolvida. Quem tem o método original de verificação deveria rodá-lo de novo no box.
4. **Bloco 4**: sem artefato Jetson para fogo — se a feira for demonstrar fogo, esse trabalho não começou.
5. Os 4 itens do Bloco 3 marcados `⚠️ parcial`/`⛔ não` que não viraram issue própria (agendamento de módulo por câmera, retenção por câmera, envio de relatório por e-mail, refresh token de device) já aparecem documentados no próprio código como gaps conhecidos — decidir se merecem issue é chamada de produto, não achado novo.

### Próximo comando

```
gh pr view 927 --repo logikos33/Recognition
```

Se `Tests (pytest)` estiver verde, o PR do Bloco 0 está pronto para merge — é o item que fecha a garantia "runner não some se o box sair".
